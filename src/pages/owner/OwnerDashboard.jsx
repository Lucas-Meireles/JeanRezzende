import { CalendarDays, Clock3, MessageSquare, TrendingUp, Users, ArrowUpRight, CheckCircle2 } from 'lucide-react';
import { Reveal } from '../../components/motion/Reveal';
import { LiquidButton } from '../../components/motion/LiquidButton';
import { useAppStore } from '../../store/useAppStore';
export default function OwnerDashboard() { const shows = useAppStore(s => s.shows), tickets = useAppStore(s => s.tickets); const metrics = [['Shows', shows.length, CalendarDays], ['Contratações', 8, Users], ['Chamados', tickets.length, MessageSquare], ['Pendências', 4, Clock3]]; return <div className="dashboard-page"><Reveal><span className="eyebrow">CENTRAL DE COMANDO</span><h1>Bom dia, Lucas.</h1><p className="page-lead">Veja o que precisa da sua atenção e controle a operação da dupla.</p></Reveal><div className="command-grid">{metrics.map(([l, v, I], i) => <Reveal key={l} delay={i * .05} className="metric-card"><I size={19}/><span>{l}</span><strong>{v}</strong></Reveal>)}</div><div className="dashboard-grid"><Reveal className="panel attention-panel"><div className="panel-heading"><div><span className="eyebrow">PRECISA DE ATENÇÃO</span><h2>Próximas ações</h2></div><CheckCircle2 size={21}/></div>{['2 contratações aguardando resposta', '1 flyer aguardando aprovação', '1 show sem endereço confirmado'].map(x => <div className="attention-row" key={x}><i className="status-dot"/><p>{x}</p><ArrowUpRight size={16}/></div>)}</Reveal><Reveal className="panel performance-panel"><div className="panel-heading"><div><span className="eyebrow">DESEMPENHO</span><h2>Site & conversão</h2></div><TrendingUp size={21}/></div><div className="fake-chart">{[35, 52, 46, 71, 64, 88, 78].map((h, i) => <span key={i} style={{ height: `${h}%` }}/>)}</div><div className="chart-labels"><span>visitas</span><span>contatos</span><span>conversões</span></div></Reveal></div><Reveal className="panel quick-panel"><div className="panel-heading"><div><span className="eyebrow">AÇÕES RÁPIDAS</span><h2>Comande sem procurar</h2></div><kbd>Ctrl K</kbd></div><div className="quick-grid"><LiquidButton href="/gestao/shows">Novo show</LiquidButton><LiquidButton href="/gestao/flyer" variant="secondary">Criar flyer</LiquidButton><LiquidButton href="/gestao/historia" variant="secondary">Novo capítulo</LiquidButton><LiquidButton href="/gestao/suporte" variant="secondary">Responder chamado</LiquidButton></div></Reveal></div>; }
