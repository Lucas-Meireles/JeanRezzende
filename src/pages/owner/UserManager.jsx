import { ShieldCheck, UserCog } from 'lucide-react';
import { Reveal } from '../../components/motion/Reveal';
import { useAppStore } from '../../store/useAppStore';
export default function UserManager() { const users = useAppStore(s => s.users), update = useAppStore(s => s.updateUser); return <div className="manager-page"><Reveal><span className="eyebrow">EQUIPE</span><h1>Usuários & permissões</h1><p className="page-lead">Controle acesso por módulo e ação sem misturar contextos.</p></Reveal><div className="panel table-panel"><div className="panel-heading"><h2>Equipe</h2><UserCog size={20}/></div><div className="table-scroll"><table><thead><tr><th>Usuário</th><th>Perfil</th><th>Status</th><th>Ação</th></tr></thead><tbody>{users.map(u => <tr key={u.id}><td><strong>{u.name}</strong><span>{u.email}</span></td><td><span className="role-pill">{u.role}</span></td><td><span className="status-pill">{u.status}</span></td><td>{u.role === 'owner' ? <span className="locked-owner"><ShieldCheck size={14}/> Owner protegido</span> : <select value={u.role} onChange={e => { if (confirm('Tem certeza que deseja alterar este perfil?'))
    update(u.id, { role: e.target.value }); }}><option>admin</option><option>artist</option><option>support</option><option>business</option></select>}</td></tr>)}</tbody></table></div></div></div>; }
