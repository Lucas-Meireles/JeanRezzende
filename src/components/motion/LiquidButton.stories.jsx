import { LiquidButton } from './LiquidButton';
export default { title: 'Motion/LiquidButton', component: LiquidButton };
export const Primary = { args: { children: 'Contratar a dupla' } };
export const Secondary = { args: { children: 'Ver agenda', variant: 'secondary' } };
